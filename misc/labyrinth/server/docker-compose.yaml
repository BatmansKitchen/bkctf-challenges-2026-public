services:
  web:
    build: .
    container_name: python_app
    ports:
      - "0.0.0.0:5000:5000"
    volumes:
      - .:/code
    environment:
      - PYTHONUNBUFFERED=1
    restart: unless-stopped